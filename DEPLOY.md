# 🚀 Podzemka Bot - Руководство по развертыванию

## 📁 Структура проекта

```
tg-bot/
├── bot/                      # Основной пакет бота
│   ├── __init__.py          # Инициализация пакета
│   ├── config.py            # Конфигурация и переменные окружения
│   ├── app.py               # Фабрика приложения
│   ├── database/            # Слой базы данных
│   │   ├── __init__.py
│   │   ├── connection.py    # Подключение и retry логика
│   │   └── queries.py       # SQL запросы
│   ├── models/              # Модели данных и FSM
│   │   ├── __init__.py
│   │   ├── states.py        # FSM состояния
│   │   └── cache.py         # Кэш в памяти
│   ├── middleware/          # Middleware и безопасность
│   │   ├── __init__.py
│   │   └── security.py      # Проверки бана, безопасность
│   ├── utils/               # Утилиты
│   │   ├── __init__.py
│   │   ├── ui.py            # UI хелперы
│   │   └── helpers.py       # Прочие хелперы
│   └── handlers/            # Обработчики команд
│       ├── __init__.py
│       ├── user.py          # Команды пользователей
│       ├── admin.py         # Команды админа
│       ├── registration.py  # Регистрация
│       └── callbacks.py     # Callback обработчики
├── main.py                   # 🎯 Точка входа (запускаем этот файл!)
├── run_dev.py                # 🔧 Авто-рестарт для разработки
├── requirements.txt          # 📦 Зависимости Python
├── .env                      # 🔐 Переменные окружения (НЕ КОММИТИТЬ!)
├── isrgrootx1.pem           # 🔒 SSL сертификат для TiDB
└── server.py.backup         # 💾 Бэкап старого кода
```

---

## 🏠 Локальная разработка

### 1. Установка зависимостей

```powershell
# Создать виртуальное окружение (рекомендуется)
python -m venv venv
.\venv\Scripts\Activate.ps1

# Установить зависимости
pip install -r requirements.txt
```

### 2. Запуск с авто-рестартом (РЕКОМЕНДУЕТСЯ)

```powershell
# Этот скрипт автоматически перезапустит бота при изменении .py, .env, .pem файлов
python run_dev.py
```

**Как это работает:**
- Скрипт отслеживает все `.py`, `.env`, `.pem` файлы
- При изменении любого файла - автоматически перезапускает бота
- Удобно для разработки - просто сохраняй файлы и бот сам перезагружается

### 3. Обычный запуск (без авто-рестарта)

```powershell
python main.py
```

### 4. Проверка работы

- Бот запустится на порту `8080` (или из переменной `PORT`)
- В логах должно быть: `✅ УСПЕХ: БД подключена`
- Открой Telegram и напиши боту `/start`
- Health check: `http://localhost:8080/check`

---

## 📦 Деплой на продакшн

### Вариант 1: Render / Heroku / Railway

#### 1. Подготовка файлов

Убедись что есть все файлы:
- ✅ `main.py` - точка входа
- ✅ `requirements.txt` - зависимости
- ✅ `.env` - переменные (будут в настройках платформы)
- ✅ `isrgrootx1.pem` - SSL сертификат

#### 2. Настройка на платформе

**Render.com (рекомендуется):**
1. Создай новый **Web Service**
2. Подключи GitHub репозиторий
3. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Environment**: Python 3.10+
4. Добавь переменные окружения из `.env`:
   - `TG_BOT_TOKEN`
   - `ADMIN_ID`
   - `TIDB_HOST`
   - `TIDB_USER`
   - `TIDB_PASSWORD`
   - `TIDB_DB_NAME`
   - `PHOTO_FILE_ID`
   - `REQUEST_PHOTO_FILE_ID`
   - `MINE_SCRIPT_BANNER_ID`
   - `MINE_SCRIPT_FILE_ID`
   - `PORT` (обычно автоматически)

#### 3. Деплой

```bash
# Закоммить изменения
git add .
git commit -m "Deploy bot v2.0"
git push origin main

# Платформа автоматически задеплоит
```

---

### Вариант 2: VPS (Ubuntu/Debian)

#### 1. Подключение к серверу

```bash
ssh user@your-server-ip
```

#### 2. Установка зависимостей

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить Python 3.10+
sudo apt install python3 python3-pip python3-venv -y
```

#### 3. Загрузка проекта

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/tg-bot.git
cd tg-bot

# ИЛИ загрузить через SFTP/SCP
# scp -r ./tg-bot user@server:/path/to/tg-bot
```

#### 4. Настройка виртуального окружения

```bash
# Создать venv
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

#### 5. Создать .env файл

```bash
nano .env
```

Вставь свои переменные:
```env
TG_BOT_TOKEN=your_token_here
ADMIN_ID=your_admin_id
TIDB_HOST=your_tidb_host
TIDB_USER=your_user
TIDB_PASSWORD=your_password
TIDB_DB_NAME=test
PHOTO_FILE_ID=your_photo_id
REQUEST_PHOTO_FILE_ID=your_request_photo_id
MINE_SCRIPT_BANNER_ID=your_banner_id
MINE_SCRIPT_FILE_ID=your_file_id
PORT=8080
```

#### 6. Настроить systemd сервис

```bash
sudo nano /etc/systemd/system/podzemka-bot.service
```

Вставь:
```ini
[Unit]
Description=Podzemka Telegram Bot
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/tg-bot
Environment="PATH=/path/to/tg-bot/venv/bin"
ExecStart=/path/to/tg-bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### 7. Запуск сервиса

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Запустить бота
sudo systemctl start podzemka-bot

# Автозапуск при старте системы
sudo systemctl enable podzemka-bot

# Проверить статус
sudo systemctl status podzemka-bot

# Посмотреть логи
sudo journalctl -u podzemka-bot -f
```

#### 8. Обновление бота

```bash
cd /path/to/tg-bot
git pull origin main
sudo systemctl restart podzemka-bot
```

---

## 🔄 Перезапуск бота при изменениях

### На локальной машине

**Используй `run_dev.py`** - он уже настроен!

```powershell
python run_dev.py
```

### На продакшене (VPS)

**Вариант A: Автоматический перезапуск при обновлении кода**

Создай скрипт `auto_deploy.sh`:

```bash
#!/bin/bash
cd /path/to/tg-bot
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart podzemka-bot
```

Настрой GitHub Webhook или cron:
```bash
# Проверять обновления каждую минуту
* * * * * /path/to/auto_deploy.sh
```

**Вариант B: Ручной рестарт**

```bash
sudo systemctl restart podzemka-bot
```

---

## 🐛 Отладка

### Проверить логи (VPS)

```bash
# Последние 100 строк
sudo journalctl -u podzemka-bot -n 100

# Логи в реальном времени
sudo journalctl -u podzemka-bot -f
```

### Проверить логи (локально)

Логи выводятся прямо в консоль где запущен `python run_dev.py` или `python main.py`

### Частые проблемы

**1. "БД недоступна"**
- Проверь переменные `TIDB_*` в `.env`
- Проверь что `isrgrootx1.pem` существует
- Проверь интернет соединение

**2. Бот не отвечает**
- Проверь `TG_BOT_TOKEN` в `.env`
- Убедись что бот запущен: `sudo systemctl status podzemka-bot`
- Проверь логи

**3. "Module not found"**
- Переустанови зависимости: `pip install -r requirements.txt`
- Проверь что находишься в правильной директории

---

## 📝 Полезные команды

```powershell
# Локальная разработка (Windows)
python run_dev.py                    # С авто-рестартом
python main.py                       # Обычный запуск

# VPS (Linux)
sudo systemctl start podzemka-bot    # Запустить
sudo systemctl stop podzemka-bot     # Остановить
sudo systemctl restart podzemka-bot  # Перезапустить
sudo systemctl status podzemka-bot   # Статус
sudo journalctl -u podzemka-bot -f   # Логи в реальном времени

# Обновление на VPS
cd /path/to/tg-bot
git pull
sudo systemctl restart podzemka-bot
```

---

## 🎯 Итого

### Для разработки
```powershell
python run_dev.py
```
✅ Автоматический перезапуск при изменениях
✅ Логи в консоли

### Для продакшена
- **Render/Heroku**: `git push` → автоматический деплой
- **VPS**: `systemd` сервис → автозапуск и мониторинг

---

**Вопросы?** Проверь логи, `.env` файл и что все зависимости установлены!
